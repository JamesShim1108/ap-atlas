{
  "name": "page-one",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-content.mjs",
    "validate": "node scripts/build-content.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run validate && npm test && npm run format:check",
    "image": "node scripts/prepare-image.mjs"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "postcss": "^8.5.0",
    "sharp": "^0.34.0"
  }
}
